import './Notification.scss';

const Notification = () => {
    return (
        <div className="notification-container">
            <p className="message">Some notification message</p>
        </div>
    );
};

export default Notification;