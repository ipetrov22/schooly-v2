import AskForm from '../components/QuestionForms/AskForm';

const Ask = () => {
    window.scroll(0, 0);

    return (
        <div className="ask-page">
            <AskForm />
        </div>
    );
};

export default Ask;
